<Window x:Class="CodeMeter.UI.UsageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        Topmost="True">

  <Window.Resources>
    <Style x:Key="GradientBar" TargetType="ProgressBar">
      <Setter Property="Height" Value="12"/>
      <Setter Property="Maximum" Value="100"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ProgressBar">
            <Grid>
              <Border Background="#313244" CornerRadius="6"/>
              <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="6">
                <Border.Background>
                  <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#A6E3A1" Offset="0"/>
                    <GradientStop Color="#F9E2AF" Offset="0.6"/>
                    <GradientStop Color="#F38BA8" Offset="1"/>
                  </LinearGradientBrush>
                </Border.Background>
              </Border>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <!-- Outer shadow effect -->
  <Border Background="#1E1E2E" CornerRadius="10" Width="268">
    <Border.Effect>
      <DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="4" Opacity="0.55"/>
    </Border.Effect>

    <StackPanel Margin="16">

      <!-- Title bar — drag handle -->
      <Grid Margin="0,0,0,14"
            MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
            Background="Transparent"
            Cursor="SizeAll">
        <TextBlock Text="CLAUDE CODE USAGE" Foreground="#CDD6F4"
                   FontSize="11" FontWeight="SemiBold"
                   VerticalAlignment="Center"/>
        <!-- Close button — stops event bubble to prevent triggering drag -->
        <Border HorizontalAlignment="Right"
                Padding="4,2"
                CornerRadius="4"
                Background="Transparent"
                Cursor="Hand"
                MouseLeftButtonDown="CloseBtn_MouseDown"
                MouseLeftButtonUp="CloseBtn_Click"
                x:Name="CloseBtn">
          <TextBlock Text="✕" Foreground="#585B70" FontSize="11"/>
        </Border>
      </Grid>

      <!-- 5-hour rolling window row -->
      <Grid Margin="0,0,0,4">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="5-Hour" Foreground="#A6ADC8" FontSize="11" VerticalAlignment="Bottom"/>
        <TextBlock Grid.Column="1"
                   Text="{Binding FiveHourPercent, StringFormat={}{0:F0}%}"
                   Foreground="#F38BA8" FontSize="18" FontWeight="Bold"/>
      </Grid>
      <ProgressBar Style="{StaticResource GradientBar}"
                   Value="{Binding FiveHourPercent, Mode=OneWay}" Margin="0,0,0,4"/>
      <TextBlock Text="{Binding FiveHourResetText}"
                 Foreground="#585B70" FontSize="10" Margin="0,0,0,14"/>

      <!-- Footer -->
      <Separator Background="#313244" Margin="0,0,0,10"/>
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="LastUpdatedText" Foreground="#585B70" FontSize="10"
                   VerticalAlignment="Center"/>
        <TextBlock Grid.Column="1" Foreground="#89B4FA" FontSize="10"
                   Cursor="Hand" MouseLeftButtonUp="Settings_Click">⚙ Settings</TextBlock>
      </Grid>

    </StackPanel>
  </Border>
</Window>
